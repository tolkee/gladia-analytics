# Repository instructions

- Before starting any code task, always read the top-level `README.md`.
- Before developing in `apps/backend`, also read `apps/backend/README.md`.
- Before developing in `apps/web`, also read `apps/web/README.md`.
- Before ending any work, always run `bun run quality:fix` from the repository root.
