{
  "name": "@gladia-analytics/backend",
  "private": true,
  "imports": {
    "#features/*": "./src/features/*/index.ts",
    "#schemas/*": "./src/features/*/*.schema.ts",
    "#lib/*": "./src/lib/*.ts"
  },
  "exports": {
    "./api-type": {
      "types": "./src/api/index.ts"
    }
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bun tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed:transcriptions": "bun run scripts/seed-transcriptions.ts",
    "infra:up": "docker compose -p gladia-analytics up",
    "infra:up:d": "docker compose -p gladia-analytics up -d",
    "infra:down": "docker compose -p gladia-analytics down",
    "infra:logs": "docker compose -p gladia-analytics logs"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.6.25",
    "@gladia-analytics/common": "workspace:*",
    "@streamparser/json-whatwg": "^0.0.26",
    "better-auth": "^1.6.25",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.33",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@gladia-analytics/ts-config": "workspace:*",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "postgres": "^3.4.9"
  }
}
